{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=src uv run python -m ecommerce_api --reload",
    "start": "PYTHONPATH=src uv run python -m ecommerce_api",
    "lint": "uv run ruff check . && uv run ruff format --check . && PYTHONPATH=src uv run lint-imports && uv run python scripts/check_sources.py",
    "lint:fix": "uv run ruff check --fix . && uv run ruff format .",
    "typecheck": "uv run mypy",
    "test": "uv run pytest",
    "db:migrate": "uv run alembic upgrade head"
  }
}

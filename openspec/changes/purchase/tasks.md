# Tasks

## 1. Shared schemas

- [ ] 1.1 Add `packages/shared/src/order/order.schema.ts` with `orderItemSchema`, `checkoutCustomerSchema`, `paymentCardSchema` (`cardNumber` stripped of spaces, 13–19 digits, Luhn; `expiry` `MM/YY` not before the current month, with the reference date injectable; `cvc` 3–4 digits), `placeOrderSchema` (1–50 items, duplicate `productId` rejected on path `items`), `orderStatuses`, `orderLineSchema`, `orderResponseSchema`, `orderSummarySchema`, `unavailableItemSchema`, `unavailableItemsBodySchema` and inferred types; export them from `index.ts`; verify `order.schema.test.ts` covers Luhn pass/fail, spaces, `01/20`, `12/99`, malformed expiry, quantity `0` and `101`, `0` and `51` items, the duplicate product, an invalid email, and passes with `pnpm --filter @ecommerce/shared test`
- [ ] 1.2 Rebuild shared (`pnpm --filter @ecommerce/shared build`) and verify `pnpm --filter api typecheck` and `pnpm --filter web typecheck` still pass

## 2. Database

- [ ] 2.1 Add the `OrderStatus` enum and the `Order` and `OrderLine` models to `apps/api/prisma/schema.prisma` (per design D4, `Product` gains `orderLines OrderLine[]`) and create migration `orders` with `prisma migrate dev`; verify the migration SQL creates the enum, both tables, the `orderId` and `productId` foreign keys and the `createdAt`, `orderId`, `productId` indexes
- [ ] 2.2 Regenerate the Prisma client, add `"Order", "OrderLine"` to the `TRUNCATE` in `apps/api/test/support/db.ts`, and verify `pnpm --filter api typecheck` passes and the existing integration tests still run green

## 3. API domain and application layers

- [ ] 3.1 Add `domain/order/order.ts` (`OrderStatus`, `OrderLine`, `Order`, `OrderSummary`, `computeTotals` in integer cents) and `UnavailableItemsError` to `domain/shared/domain-error.ts`; verify `domain/order/order.test.ts` asserts `3 × 19.99 = 59.97`, a multi-line total and a zero-price line
- [ ] 3.2 Add `application/ports/order-repository.ts` (`OrderDraft` with customer, `cardLast4` and items; `OrderRepository` with `reserve`, `settle`, `findById`, `findAll`; `ORDER_REPOSITORY`) and `application/ports/payment-gateway.ts` (`ChargeRequest`, `ChargeResult`, `PaymentGateway`, `PAYMENT_GATEWAY`); verify typecheck
- [ ] 3.3 Add `application/orders/__fakes__/in-memory-order-repository.ts` (reserves against `InMemoryProductRepository`, collecting every short or unavailable item before throwing, snapshotting lines from the product) and `__fakes__/fake-payment-gateway.ts` (scripted `approved`/`declined`/throw); verify typecheck
- [ ] 3.4 Implement `PlaceOrder` (reserve → charge → settle, a throwing gateway settles as `payment_failed` with "Payment provider unavailable"), `ListOrders` and `GetOrder` (`NotFoundError('order', id)`); verify `application/orders/orders.test.ts` covers stock decremented on `paid`, stock restored and reason recorded on declined, provider failure, one short item rejecting the whole order with every problem listed and stock untouched, deleted and unknown products as `unavailable`, price snapshot and totals, newest-first listing, and 404

## 4. API infrastructure

- [ ] 4.1 Implement `infra/persistence/prisma/prisma-order.repository.ts` with `order-mapper.ts`: `reserve` as one interactive transaction using conditional `updateMany` decrements on items sorted by `productId`, re-reading failed items for `available`/`reason`, then `order.create` with lines; `settle` as one transaction that sets the status and payment fields and, on decline, increments stock back; `findById`/`findAll` (newest first, summaries with `itemCount`); verify `test/prisma-order.repository.integration.test.ts` covers rollback of every decrement when one item is short, `available` `0` for a deleted product, stock restored by a declined settle, and three concurrent reservations on `stock` `1` yielding exactly one success and `stock` `0`
- [ ] 4.2 Add `infra/payments/fake-payment-gateway.ts` (`4000000000000002` → "Your card was declined", `4000000000009995` → "Your card has insufficient funds", otherwise approved with `fake_<random>`); verify `fake-payment-gateway.test.ts` covers the three outcomes
- [ ] 4.3 Add `OrdersController` (`POST /orders` with `ZodValidationPipe(placeOrderSchema)` and `201`; `GET /orders`; `GET /orders/:id` with `OrderIdPipe`), `OrdersModule` binding `ORDER_REPOSITORY`, `PAYMENT_GATEWAY` and the use cases, map `UnavailableItemsError` to `409 { message, items }` in `DomainExceptionFilter` (with a unit test), and register the module in `app.module.ts`; verify `test/orders.integration.test.ts` covers `201 paid` with stock decremented and `cardLast4` `4242`, `201 payment_failed` with stock restored for `4000 0000 0000 0002` and `4000 0000 0000 9995`, `409` listing only the short item with `A` untouched, `409 unavailable` for a deleted and for an unknown product, `400` for a Luhn failure, an expired card, quantity `0`, a duplicate product and combined customer/card issues, price snapshot after a `PATCH`, order intact after the product's `DELETE`, `GET /orders` newest first without `lines`, no card number anywhere in `GET /orders/:id`, and `404`
- [ ] 4.4 Run `pnpm --filter api test` and `pnpm --filter api lint` and verify both pass

## 5. Web: cart

- [ ] 5.1 Add `src/lib/cart-store.ts` (`localStorage` key `cart`, `useSyncExternalStore` with an empty server snapshot, guarded storage access with in-memory fallback, `add`, `setQuantity`, `remove`, `clear`, `applyUnavailable`, `useCart`, `useCartCount`); verify `cart-store.test.ts` covers accumulation on repeated add, quantity edits, removal, clearing, `applyUnavailable` removing `unavailable` items and capping `insufficient_stock` ones, persistence across a fresh store instance, and a throwing `localStorage`
- [ ] 5.2 Add `src/components/cart/add-to-cart-button.tsx` and use it in `ProductsTable` (quantity 1) and `ProductDetailPage` (with a quantity input bounded by `stock`); disabled and labelled `Out of stock` when `stock` is `0`; verify `add-to-cart-button.test.tsx` and the updated table and detail tests cover the add, the accumulated quantity and the disabled state
- [ ] 5.3 Add `Orders` to the `SiteHeader` links and a `CartLink` client component showing the item count only when the cart is not empty; verify `site-header.test.tsx` asserts the `Products`, `Imports`, `Orders` links, the cart link without a count when empty and with `3` for quantities `1` and `2`
- [ ] 5.4 Add `/cart` (`CartPage`: lines table with name link, unit price, quantity input, line total, remove; total; `Checkout` button; empty state linking to products); verify `cart-page.test.tsx` covers the rendered lines and total, a quantity edit, removal, and the empty state without a checkout action

## 6. Web: checkout and orders

- [ ] 6.1 Add `src/lib/orders-api.ts` (`placeOrder`, `listOrders`, `getOrder`, `orderKeys`); verify typecheck
- [ ] 6.2 Add `/checkout` (`CheckoutPage` with the summary aside and `CheckoutForm` using `zodResolver(placeOrderSchema)` over customer and card, items from the store): on `paid` clear the cart, invalidate `productKeys.all` and `orderKeys.all` and navigate to `/orders/[id]`; on `payment_failed` show the reason with a link to the order and keep cart and form; on `409` apply the items to the cart and name each one; on `400` set field errors; empty cart shows the cart's empty state; verify `checkout-page.test.tsx` covers the posted body (digits-only card number, items from the cart), navigation and cleared cart on `paid`, the decline message with the cart kept, the `409` adjustment with quantity lowered to `1`, and the blocked submission for an invalid email and card number `1234`
- [ ] 6.3 Add `/orders` (`OrdersPage`: table with short code, date, customer, items, total, status badge; skeleton, empty and error states; rows linking to the order); verify `orders-page.test.tsx` covers the list including a `payment_failed` order, and the empty and error states
- [ ] 6.4 Add `/orders/[id]` (`OrderPage`: short code heading, status badge, customer and payment in the spec-sheet grid with the card's last four digits and the reference or decline reason, lines table, total; not-found state linking to `/orders`); verify `order-page.test.tsx` covers a paid order, a failed order showing its reason, and the not-found state
- [ ] 6.5 Run `pnpm --filter web test`, `pnpm --filter web lint` and `pnpm --filter web typecheck` and verify all pass

## 7. Docs and final check

- [ ] 7.1 Update `README.md`: a "Purchase" section (cart → checkout → order flow, the test card numbers and their outcomes, the `409` body, the endpoints and pages), "Purchase" entries in Decisions linking to this design (two-transaction reservation, decline as an outcome, fake gateway behind a port, no card data stored, snapshot lines), and the `purchase` row of the Status table; verify the links resolve
- [ ] 7.2 Run `pnpm check` at the root and verify every task is green
- [ ] 7.3 Run `docker compose up --build` from a clean volume, import `data/e-commerce_input.csv`, add two products to the cart, check out with `4242 4242 4242 4242` and verify the order page shows `paid` and the products' stock dropped; check out again with `4000 0000 0000 0002` and verify the decline message, the cart kept and the stock unchanged; verify `/orders` lists both

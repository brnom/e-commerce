# Tasks

## 1. Shared schemas

- [x] 1.1 Add `packages/shared/src/product/product.schema.ts` with `createProductSchema` (sku trimmed + upper-cased, name, description, price with two-fraction-digit check, stock, weightKg, category), `updateProductSchema` (`.partial()` with `category: null` allowed), `listProductsQuerySchema` (q, category, sort, order, page, limit with defaults and bounds) and `productResponseSchema`; export them and their inferred types from `src/index.ts`; verify `pnpm --filter @ecommerce/shared typecheck` passes
- [x] 1.2 Add `product.schema.test.ts` covering: blank name rejected, `"$29.99"` price rejected, `-5` stock rejected, `0` price with missing weight accepted, `sku` `" rs-001 "` normalized to `RS-001`, `29.999` price rejected, `limit=500` rejected, list defaults applied; verify `pnpm --filter @ecommerce/shared test` passes

## 2. Database

- [x] 2.1 Add `Category` and `Product` models to `apps/api/prisma/schema.prisma` per design D1 (uuid v7 ids, `@db.Citext` category name, `Decimal(12,2)` price, `Decimal(8,3)` weight, `deletedAt`, unique `sku`, GIN trigram index on name and description, FK `ON DELETE SET NULL`); verify `pnpm --filter api db:generate` succeeds
- [x] 2.2 Create migration `product_catalog` with `prisma migrate dev --create-only`, prepend `CREATE EXTENSION IF NOT EXISTS citext; CREATE EXTENSION IF NOT EXISTS pg_trgm;`, then apply it; verify `prisma migrate dev` reports no drift and `psql \d "Product"` shows the GIN index and the unique SKU index
- [x] 2.3 Add `TEST_DATABASE_URL` (default `postgresql://app:app@localhost:5432/ecommerce_test`) to `.env.example`, the README env table and `turbo.json` `test.env`; add a Vitest global setup for the `integration` project that creates the database if missing and runs `prisma migrate deploy` against it, plus a `test/support/db.ts` helper that truncates `Product` and `Category`; verify `pnpm --filter api vitest run --project integration` boots against the test database with `docker compose up db -d` running

## 3. API domain and application layers

- [x] 3.1 Add `domain/product/product.ts` (`Product`, `Category` types, `normalizeSku`) and `ConflictError(field, value)` in `domain/shared/domain-error.ts`; extend `DomainExceptionFilter` to map it to `409 { message, field, value }` with a unit test; verify `pnpm --filter api vitest run --project unit` passes
- [x] 3.2 Add ports `application/ports/product-repository.ts` (`create`, `findById`, `update`, `softDelete`, `search`) and `application/ports/category-repository.ts` (`findOrCreate`, `findAll`) with `Symbol` tokens; verify `pnpm --filter api typecheck` passes and `pnpm --filter api lint` shows no layer violation
- [x] 3.3 Add use cases `CreateProduct`, `GetProduct`, `UpdateProduct`, `DeleteProduct`, `ListProducts`, `ListCategories` in `application/products/` as plain classes taking the ports; add in-memory fakes under `application/products/__fakes__/` and unit tests covering: create resolves a category by name, update with `category: null` clears it, update without `category` keeps it, delete of unknown id throws `NotFoundError`, get of deleted id throws `NotFoundError`; verify `pnpm --filter api vitest run --project unit` passes

## 4. API infrastructure

- [x] 4.1 Implement `PrismaCategoryRepository` (`findOrCreate` via `upsert` on `name`, `findAll` ordered by name) and an integration test proving `" electronics "` reuses `Electronics` and `GET`-side listing returns it once; verify `pnpm --filter api vitest run --project integration` passes
- [x] 4.2 Implement `PrismaProductRepository` with `deletedAt: null` on every read, `Decimal` ↔ `number` mapping, `P2002` → `ConflictError("sku", value)`, `softDelete` via `updateMany` count, `search` with `contains`/`mode: "insensitive"` on name or description, category filter, sort/order, `skip`/`take` and a `count` in the same transaction; integration tests covering: duplicate SKU on create → `ConflictError`, update to another product's SKU → `ConflictError`, update keeping own SKU succeeds, `q=MOUSE` matches description case-insensitively, `q=100%` matches only the literal `100%` product, category + q combined, `total` counts beyond the page (45 rows, page 3 limit 20 → 5 items), sort by price ascending, deleted rows excluded, deleted SKU stays reserved, second `softDelete` returns not found; verify `pnpm --filter api vitest run --project integration` passes
- [x] 4.3 Add `ProductsController` (`POST /products` 201, `GET /products`, `GET /products/:id`, `PATCH /products/:id`, `DELETE /products/:id` 204) using `ZodValidationPipe` with the shared schemas for body and query, `CategoriesController` (`GET /categories`), and `ProductsModule` binding both ports and providing use cases via `useFactory`; register in `app.module.ts`; verify `pnpm --filter api build` succeeds and `curl` against `pnpm dev` returns `201` for a valid create and `400` with an `issues` array for a blank name
- [x] 4.4 Add `test/products.integration.test.ts` booting `AppModule` against the test database and covering the HTTP contract: create returns generated id and normalized SKU, several invalid fields reported together, `409` body names the SKU, partial update leaves other fields intact, unknown id `404`, delete then read `404`, delete twice `404`, `limit=500` → `400`, list response shape `{ items, total, page, limit }`; verify `pnpm --filter api test` passes

## 5. Web

- [x] 5.1 Add `react-hook-form` and `@hookform/resolvers` to `apps/web`; add `src/lib/products-api.ts` with typed functions over `apiClient` for the six endpoints and query-key helpers; verify `pnpm --filter web typecheck` passes
- [x] 5.2 Build `/products` (`src/app/products/page.tsx` + `ProductsTable`, `ProductFilters`, `Pagination` components): filters read from and written to URL search params (`router.replace`, debounced search input), category `<select>` fed by `GET /categories`, sortable column headers, page navigation, total count, inline two-step delete with `useMutation` invalidating `["products"]`; verify with `pnpm dev` that typing `mouse` narrows the list and the URL carries `?q=mouse`, and that a refresh reproduces the same list
- [x] 5.3 Build `ProductForm` with `react-hook-form` + `zodResolver(createProductSchema)`, `valueAsNumber` on numeric inputs, category `<datalist>` from `GET /categories`, and mapping of `ApiError` `400 issues[]` / `409 field` into `setError`; use it in `/products/new` (create, then navigate to `/products`) and `/products/[id]/edit` (load via `GET /products/:id`, submit `PATCH`); verify with `pnpm dev` that an empty name and `-1` price show field messages without a request, and that a duplicate SKU shows the API message next to the SKU field
- [x] 5.4 Link the home page to `/products`; add a `no-restricted-syntax` lint rule for the `dangerouslySetInnerHTML` JSX attribute in `eslint.config.mjs` (web scope); verify a temporary `dangerouslySetInnerHTML` in a component fails `pnpm --filter web lint`, then remove it
- [x] 5.5 Add web tests (`@testing-library/react` + `user-event`, `fetch` stubbed): `ProductForm` blocks submission and shows messages for empty name and negative price with no fetch call; a stubbed `409` response places the message on the SKU field; delete control requires confirmation and sends no request when cancelled; a product named `<script>alert('xss')</script>` renders as text; verify `pnpm --filter web test` passes

## 6. CI, docs and final check

- [x] 6.1 Add a `postgres:16-alpine` service with a health check to the quality-gate job in `.github/workflows/ci.yml`, set `TEST_DATABASE_URL` for the test step; verify `actionlint` passes (via docker as in the scaffold) and the workflow is green on the pull request
- [x] 6.2 Update `README.md`: "Run for development" notes the test database prerequisite, the env table gains `TEST_DATABASE_URL`, the Decisions section summarizes D1–D8 (citext categories, soft delete with reserved SKU, decimal-to-number, ILIKE + trigram over full-text, real-database integration tests), and the Status table marks `products-crud-search` in progress; verify `pnpm format:check` passes
- [x] 6.3 Run `pnpm check` and `docker compose up --build` from a clean state; verify both succeed, that `http://localhost:3000/products` lists a product created through the form, and that the API container log shows the `product_catalog` migration applied before "Nest application successfully started"
